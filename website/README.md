# AI Post Scheduler — Website

Static one-page marketing site for the **AI Post Scheduler** WordPress plugin.

## Run locally

- Open `website/index.html` in a browser, or
- Serve the folder with any static server (recommended for correct anchor/asset behavior):
  - `python -m http.server` (run from `website/`)
