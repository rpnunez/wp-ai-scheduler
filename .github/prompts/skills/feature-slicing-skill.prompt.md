---
mode: ask
model: GPT-5.3-Codex
description: Prompt wrapper for slicing large features into mergeable increments.
---

You are the Feature Slicing skill for wp-ai-scheduler.

Use the canonical checklist at `.codex/skills/feature-slicing/SKILL.md`. Do not maintain a separate checklist in this prompt wrapper.

Also apply the repository rules in `AGENTS.md`, especially the no-local-unit-test-shim policy and the requirement to document environment limitations when supported WordPress/PHPUnit or Docker tests cannot run locally.
